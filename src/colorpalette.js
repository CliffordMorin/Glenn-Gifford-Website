import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#33383b",
    },
    secondary: {
      main: "#ba000d",
    },
    background: {
      paper: "#fff",
      default: "#1d2026",
    },
  },
});

export default theme;
